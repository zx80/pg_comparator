* bugs
- provide meaningful examples in the documentation
- test with composite key?

* general
- rename pg_table_sync or pg_sync if available?
- optimality?
- check DBD::Pg options...

* options and improvements
- update -> delete/insert
- skip the size count() somehow?
- use transactions? locks?
- use crypto hash instead of custom checksum function?
- use h(h(c1)||h(c2)) instead of h(c1||c2)
- use integer primary key without hashing?
- try create + insert from select if count directly available?
- what to count: select vs create vs drop?
- allow functions? e.g. lower(col1), trim(col2)...

* pointers
- http://www.sql-server-performance.com/articles/dba/database_comparison_sp_p1.aspx
